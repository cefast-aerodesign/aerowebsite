import styled from '@emotion/styled'
import { H2 } from '../../fonts/fonts'
import colors from '../../shared/colors'


const HeaderTitle = styled(H2)`
    color: ${colors.orange}
`

export {HeaderTitle}